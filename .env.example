# Database connection (SQLite for local, PostgreSQL for production)
DATABASE_URL="file:./dev.db"

# JWT Secret for authentication
JWT_SECRET="super-secret-avpocket-jwt-key-change-in-production-2026"

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# GitHub OAuth (Optional for local, can be configured later)
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# Storage path for uploaded plugins and assets
UPLOAD_DIR="./storage/uploads"
